==================
empty source has no layout nodes
==================

---

(program)

==================
a newline-only program retains its source layout
==================



---

(program
  (item_list
    (newline_opt
      (newline)
      (newline))))

==================
space and horizontal tab remain unnamed layout
==================
	 
---

(program)

==================
source boundary continuations preserve the top-level structure
==================
\
BEGIN {}\
 
---

(program
  (item
    (pattern
      (special_pattern
        (begin_keyword)))
    (action)))

==================
continuation-only source adds no named syntax nodes
==================
\
\
\
 
---

(program)

==================
a continuation before a raw newline preserves its terminator
==================
BEGIN {};\

BEGIN {}
---

(program
  (item_list
    (item
      (pattern
        (special_pattern
          (begin_keyword)))
      (action))
    (terminator
      (newline)))
  (item
    (pattern
      (special_pattern
        (begin_keyword)))
    (action)))

==================
continuation before a raw newline does not terminate the item by itself
==================
BEGIN {}\

BEGIN {}
---

(program
  (item_list
    (item
      (pattern
        (special_pattern
          (begin_keyword)))
      (action))
    (terminator
      (newline)))
  (item
    (pattern
      (special_pattern
        (begin_keyword)))
    (action)))
